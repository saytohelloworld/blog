0. 你需要为程序中的变量、类型、函数和标签起一个名字，我们把这个名字称之为？
    常量
    [改正:] 标识符

1. main 是一个合法的标识符吗？
    是 [main 是函数的标识符名称]

2. 字符串是常量吗？
    是

3. 在 C 语言中，转义字符 '\n' 占多大空间？
    占一个字节的空间

4. 字符串 "FishC" 由哪些字符构成？
    由五个字母 F i s h C 构成
    [改正:] 还有一个结束标志 \0

5. 我们都知道变量就是在内存里挖个坑让你存放数据，数据类型决定坑的大小。那常量有没有数据类型呢？
    有，整型、实型和字符 [只要是数据，那就有类型]

6. 如果有符号常量定义如：#define F(n) 2*n，那么请问代码中 F(3+2) 的值等于多少？
    [改正:] F(3+2) == 2*3+2 == 8，宏定义是在程序编译时先进行的预处理，做法是直接将标识符替换为常量，并不会进行相关运算。因此，直接将 F(3+2) 替换为 2*3+2